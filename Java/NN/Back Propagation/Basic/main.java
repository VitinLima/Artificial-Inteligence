public class main{
	public static void main(String[] args){
		AIConstructor ai = new AIConstructor(new int[]{1,2});

/*
		
		float[] input = new float[1];
		int fAns, sAns;
		
		for(int i = 0; i < 100; i++){
			input[0] = 1.0f;
			ai.setInput(input);
			fAns = ai.getAns();
			
			input[0] = 0.0f;
			ai.setInput(input);
			sAns = ai.getAns();
			
			if(sAns == 1) ai.backPropagate(true);
			else ai.backPropagate(false);
			
			input[0] = 0.0f;
			ai.setInput(input);
			fAns = ai.getAns();
			
			input[0] = 0.0f;
			ai.setInput(input);
			sAns = ai.getAns();
			
			if(sAns == 0) ai.backPropagate(true);
			else ai.backPropagate(false);
		}
		
		System.out.println(ai.getData());
		
		for(int i = 0; i < 5; i++){
			input[0] = 1.0f;
			ai.setInput(input);
			fAns = ai.getAns();
			
			input[0] = 0.0f;
			ai.setInput(input);
			sAns = ai.getAns();
			
			System.out.println(sAns);
			
			input[0] = 0.0f;
			ai.setInput(input);
			fAns = ai.getAns();
			
			input[0] = 0.0f;
			ai.setInput(input);
			sAns = ai.getAns();
			
			System.out.println(sAns + "\n\n");
		}
*/
	}
}